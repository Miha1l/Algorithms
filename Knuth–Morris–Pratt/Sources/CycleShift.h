#ifndef CYCLESHIFT_H
#define CYCLESHIFT_H

#include <iostream>
#include <fstream>
#include <string>

void readData(std::string& s1, std::string& s2);
int cycleShift(std::string& A, std::string& B);

#endif